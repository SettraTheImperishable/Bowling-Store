<p>
  Your search for '<%= @query %>' returned <%= pluralize(@products.size, 'result') %>.
</p>

<div>
  <%= render partial: 'product',
           collection: @products,
           locals: { name_is_link: false } %>
</div>