<p>
  Your search for '<%= @query %>'
  <% unless !@category %>
    in <%= @category.name %>
  <% end %>
  returned <%= pluralize(@products.size, 'result') %>.
</p>

<div>
  <%= render partial: 'product',
           collection: @products,
           locals: { name_is_link: false } %>
</div>